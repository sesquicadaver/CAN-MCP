# Etapa 17 — brief_ast vendoring + requirements generator

**Task:** Vendor brief_ast into codimension_core; headless generate_requirements; IDE thin wrappers.

**Outcome:** Core has zero dependency on codimension.parsers/cdmpyparser shim; IDE re-exports from core.
